{
  "name": "@domain-finder/core",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Clearance core: pure domain logic (availability taxonomy, ranking, cadence). No HTTP, no framework, no env, no DB.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./pool": "./src/pool.ts",
    "./cache": "./src/cache.ts",
    "./strings": "./src/strings.ts"
  },
  "dependencies": {
    "zod": "^4.4.3"
  },
  "scripts": {
    "test": "node --import tsx --test --test-force-exit src/*.test.ts"
  }
}
